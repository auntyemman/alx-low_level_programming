#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 * Description: prints all digits of base 10 starting from 0
 * Return: Always 0 (Success)
 */
int main(void)
{
	int r;

	for (r = 0; r <= 9; r++)
{
	printf("%d", r);
}
	printf("\n");
	return (0);
}
